# Gmail IMAP Configuration
# Enable 2FA in Google Account and generate an App Password: https://myaccount.google.com/apppasswords
IMAP_SERVER=imap.gmail.com
IMAP_PORT=993
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_here

# Custom Domain Configuration (Cloudflare Email Routing)
# Ensure you have a catch-all rule or the specific address routing to the EMAIL_USER above
# Separate multiple domains with commas, e.g.: domain1.com,domain2.com
CUSTOM_DOMAIN=your_domain.com

# Browser Configuration
# Set to 'true' to run in headless mode (no visible browser), 'false' to show browser
HEADLESS=false